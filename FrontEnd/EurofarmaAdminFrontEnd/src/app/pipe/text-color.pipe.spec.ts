import { TextColorPipe } from './text-color.pipe';

describe('TextColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TextColorPipe();
    expect(pipe).toBeTruthy();
  });
});
